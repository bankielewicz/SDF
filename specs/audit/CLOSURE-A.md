---
schema: devforgeai-audit/1
doc: closure
scope: [AUDIT-1-cli, AUDIT-2-hooks]
produced_by: closure-verifier-A
---

# Closure verification A — AUDIT-1 (CLI) and AUDIT-2 (hooks)

Every blocker and high finding of `specs/audit/AUDIT-1-cli.md` and `specs/audit/AUDIT-2-hooks.md`.
Evidence is a file path with line numbers, a test name that exists and passed in the recorded
`cargo test --no-fail-fast` run, or a command the verifier ran with its output. No fixer report was
taken as evidence. Mechanical checks 1, 5, 7 and 9 were run here; 2, 3, 4, 6 and 8 belong to the
other verifier.

Counts: AUDIT-1 **51 closed, 5 open** of 56 (30 blockers, 26 highs). AUDIT-2 **13 closed, 2 open**
of 15 (6 blockers, 9 highs). Nothing superseded.

## AUDIT-1 — cli

| ID | severity | status | evidence |
|---|---|---|---|
| CLI-001 | blocker | open | Supplementing half landed: `cli/templates/settings.hooks.json` carries a `PreToolUse` group with `"matcher": "Bash\|PowerShell"`, and `cli/src/hooks/run.rs:592` routes `Bash`/`PowerShell` into `pre_tool_use_shell` (`:796`), which resolves write targets with `shell_write_targets` (`:952-1008`) and runs the producer check on any that lands under `.devforgeai/`. Tests `pre_tool_use_bash_redirect_under_devforgeai_exits_two`, `pre_tool_use_powershell_redirect_under_devforgeai_exits_two`, `pre_tool_use_bash_unrecognised_command_exits_zero` pass. The **enforcing half the fix spec names is absent**: `stop` (`run.rs:1127-1258`) runs `gate check` and `handoff` only — no walk of `.devforgeai/`, no `[stop_hook].scanned_at`; `grep -rn "scanned_at\|modified()" src/hooks/ src/state.rs` returns nothing. See Open items. |
| CLI-002 | blocker | closed | `run.rs:1316-1319` reads `last_assistant_message` first, falls back to `agent_transcript_path`; `transcript_path` and `tool_response.content` are gone from the function. Tests `subagent_stop_registered_ingests`, `subagent_stop_reads_agent_transcript_path_when_the_message_is_absent`, `subagent_stop_with_neither_key_warns_w411` pass. |
| CLI-003 | high | closed | `run.rs:1095-1118` builds `hookSpecificOutput.additionalContext` from the diagnostics and gate lines, capped at 10 000 chars, and emits nothing when there is nothing to say; `cli/src/lib.rs:208-212` writes `hook_json` to stdout and suppresses `human` beside it. Tests `post_tool_use_invalid_document_emits_additional_context`, `post_tool_use_with_nothing_to_say_emits_no_object` pass. |
| CLI-004 | high | open | `run.rs:1188-1206` blocks again while `stop_hook_active` is true, up to `STOP_BLOCK_CAP = 3` (`run.rs:51`), and resets the budget whenever the `(session, phase, id)` chain changes (`:1193`). Test `stop_active_flag_blocks_again_below_cap` (`tests/hook_run.rs:833-855`) asserts exit 2 on the second dispatch with `stop_hook_active: true`. The spec was rewritten to match the code (`specs/01-cli.md:383, 529, 1549`: "The budget itself is three blocks"), so spec and code now agree with each other and disagree with `specs/ANTHROPIC-GUIDANCE.md:23` — "A hook must read `stop_hook_active` and exit 0 when set to avoid futile loops" — which outranks the CLI spec, and with FIX-PLAN decision 1 — "`stop_hook_active` true: … exit 0, no gate run". See Open items. |
| CLI-005 | high | closed | `run.rs:258-326`: the trust-failure `stop` branch honours `stop_hook_active`, emitting `decision: block` + `systemMessage` at exit 2 the first time and `systemMessage` alone at exit 0 on the continuation, with `[last_gate].result = "TRUST_FAIL"` written through `ctx.store_state()` whose error is folded into warnings (`:321-326`, closing CLI-014). Verified on the release binary in a temp repo: first `hook run stop` exit 2, second with `stop_hook_active: true` exit 0, `state.toml [last_gate] result = "TRUST_FAIL"`. Tests `stop_trust_failure_blocks_with_pin_command`, `stop_trust_failure_with_active_flag_exits_zero` pass. |
| CLI-006 | high | closed | `run.rs:698-706` emits `deny()` (`:80-90`: `hookSpecificOutput.permissionDecision = "deny"` with `permissionDecisionReason`) and keeps exit 2; `lib.rs:225-232` renders warnings and errors to stderr in both output modes, so `--json` no longer suppresses them (also closes CLI-215). Tests `pre_tool_use_producer_mismatch_exits_two`, `json_still_writes_diagnostics_to_stderr` pass. |
| CLI-030 | blocker | open | The tree and `cli/REVISION` now agree: an independent reimplementation of the documented framing (path + `0x00` + 8-byte BE length + bytes, byte-ascending, excluding `cli/DIGEST` and `cli/REVISION`, skipping `target/` and `.git/`) over `cli/` gives 89 files and `sha256:2c3964003698088cd73fd75552cd83b6366ae54ccf74b4033add1ee5365a71df`, which equals `cli/REVISION` line 2 exactly; `cli/DIGEST` equals `sha256sum target/release/devforgeai.exe`. Step one of the fix spec landed as a hidden subcommand rather than a `src/bin/` binary: `cli/src/cli.rs:386-396` and `cli/src/cmd/trust.rs:93-126` define `trust digest`, which prints both values from `trust::source_digest` and `trust::digest_file`, so the writer and the reader share one code path. Still open: `specs/01-cli.md:1580` and decision 39 at `:2913` describe a **git tracked-file list excluding only `cli/DIGEST`**, `trust digest` is documented in no spec, and no test binds `source_digest` to the shipped `REVISION`. See Open items. |
| CLI-040 | blocker | closed | `cli/src/doc/mod.rs:825-846`: `producer_allows` returns false for `row.producer == "devforgeai-cli"` and otherwise compares `row.producer` with `producer_of_phase_opt(current_phase)`; a phase outside the enum allows nothing. Tests `producer_check_refuses_a_report_the_binary_owns`, `producer_check_refuses_a_build_report_from_build`, `producer_check_allows_the_owning_skill` pass. |
| CLI-041 | high | closed | `cli/src/doc/ids.rs:96-120` `allocate_reserved` reserves with `OpenOptions::create_new(true)`; `cli/src/cmd/doc.rs:27` calls it and reports the reservation. Tests `allocate_twice_gives_two_ids`, `allocate_skips_a_reserved_id` pass. |
| CLI-060 | high | open | The assertion the finding asks for is still absent: `tests/hook_run.rs:140` reads `out.hook_json` off the in-process `Outcome`, and `grep -n "cargo_bin\|Command::new" tests/hook_run.rs` returns nothing, so no test checks the bytes that leave the process for any hook event. See Open items. |
| CLI-061 | high | open | `stop_different_id_resets_counter` is gone, but its replacement `stop_active_flag_blocks_again_below_cap` (`tests/hook_run.rs:847-854`) asserts exit 2 for `stop_hook_active: true`, so the suite still locks in the behaviour CLI-004 names. Tied to CLI-004. |
| CLI-062 | high | closed | `tests/hook_run.rs` now builds SubagentStop payloads from `agent_type` + `last_assistant_message`; `subagent_stop_registered_ingests`, `subagent_stop_reads_agent_transcript_path_when_the_message_is_absent`, `subagent_stop_with_neither_key_warns_w411`, `subagent_stop_malformed_envelope_blocks` all pass. |
| CLI-066 | high | closed | `producer_check_refuses_the_plan_document_from_verify`, `producer_check_refuses_a_build_report_from_build`, `producer_check_refuses_a_report_the_binary_owns` pass alongside the retained control `producer_check_design_allowed_from_any_phase`. |
| CLI-100 | blocker | closed | `gate.rs:2619-2627`: the `no_coverage_paths` skip is gone; an absent artifact is `DFA-E312`, and a coverage command that exits non-zero fails with `DFA-E312` rather than parsing a stale artifact (also closes CLI-118). Test `coverage_with_no_coverage_paths_is_e312` passes. |
| CLI-101 | blocker | closed | `gate.rs:2331-2356` `empty_selection` names the two causes; `tests_pass` refuses an empty selection with `DFA-E310` (`:2394`). Tests `tests_pass_with_a_selection_that_names_nothing_is_e310`, `tests_pass_with_no_stack_at_all_is_e310` pass. |
| CLI-102 | blocker | closed | `gate.rs:3195-3235`: non-zero exit fails `DFA-E344` with the run evidence and no parsing; lines without a tab are dropped; an empty symbol list after a successful run fails; `docs_cover_matches_a_heading_exactly` asserts the exact-heading comparison. Tests `docs_cover_fails_when_the_command_fails`, `docs_cover_ignores_a_line_with_no_tab`, `docs_cover_matches_a_heading_exactly` pass. |
| CLI-103 | blocker | closed | `gate.rs:2168-2182`: both skips are failures now — a non-story subject and a root that is not a work tree each give `DFA-E239`. Tests `files_declared_outside_a_work_tree_fails`, `files_declared_without_a_story_subject_fails` pass. |
| CLI-104 | blocker | closed | `docs` is required for `no_cycle` (`gates.rs:148`), and `gate.rs:1764-1773` raises `DFA-E345` when the expansion is empty. Tests `a_check_missing_a_required_key_is_e302`, `no_cycle_refuses_an_empty_glob_and_an_unreadable_document` pass. |
| CLI-105 | blocker | closed | `gate.rs:1774-1792`: all three silent `continue` arms are gone — `load_value` propagates, a `from` that does not resolve is `DFA-E345`, and a document defining no node is `DFA-E345`. Same test as above. |
| CLI-106 | blocker | closed | `gate.rs:2030-2053`: an absent `stories` key is `DFA-E345`, a non-sequence and an empty list are `DFA-E341`, and an entry with no `id` is `DFA-E341` naming the index. Test `release_stories_refuses_a_list_that_names_nothing` passes. |
| CLI-107 | high | closed | `gate.rs:546-549`: `NO_RUN_KINDS` names `docs_cover`; `coverage_min` takes its read-only path under `--no-run` (`:2584`), which `specs/01-cli.md:318,940` now documents, so spec and code agree. Tests `docs_cover_under_no_run_skips`, `no_run_skips_the_three_command_kinds_and_reads_coverage` pass. |
| CLI-108 | high | closed | `gate.rs:2127-2134`: `story_valid` passes `None` as the id and lets `scope` decide the set; `scope` is enum-validated at load (`gates.rs:158`). Tests `story_valid_reads_the_scope_rather_than_the_gate_subject`, `story_validate_refuses_a_bad_scope_even_with_an_id`, `an_unknown_scope_is_e012_exit_three` pass. |
| CLI-109 | high | closed | `gate.rs:1993-2004`: `compiled_floor` parses the key into its three shapes and looks the floor up through `min_ratio_floor(phase)` and `LAYER_FLOORS`; `recognised_threshold_shape` (`:2014`) refuses a threshold key it cannot locate. Tests `no_threshold_decrease_locates_the_floor_by_shape`, `no_threshold_decrease_gives_e348` pass. |
| CLI-110 | high | closed | `gate.rs:556-567` `is_check_failure_code` converts every `DFA-E1xx`, `E2xx`, `E3xx` and `E401` into a check failure, leaving only the usage, trust and internal bands to abort; `min_severity` and `scope` are enum-validated at load. Tests `a_key_outside_its_enum_is_e302`, `the_min_severity_filter_drops_a_lower_rule` pass. |
| CLI-111 | high | closed | `gate.rs:2363-2384` `run_for` converts `DFA-E319` into a `Run` with `exit_code: -1`, `duration_ms: 0` and the spawn error as output, so the report records why the command did not run. Tests `run::tests::the_evidence_mapping_carries_the_four_keys`, `the_evidence_carries_the_command_the_code_and_the_counts` pass. |
| CLI-140 | blocker | closed | `gate.rs:238` propagates the `load_value` error out of `condition_true`, and `evaluate` (`:486-496`) turns it into a check failure through `is_check_failure_code`, so an unreadable condition document never silences a block check. Test `required_when_over_an_absent_document_fails` passes. |
| CLI-141 | blocker | closed | `verifiers` is required at load (`gates.rs:146`); `gate.rs:1634-1639` fails `DFA-E316` for a present-but-empty list. Tests `a_check_missing_a_required_key_is_e302`, `verifier_pass_names_config_when_the_verifier_is_unregistered` pass. |
| CLI-142 | blocker | closed | `gate.rs:1659-1685`: `passed` and `total` are read as present integers, an absent or wrongly typed key is `DFA-E317`, and `passed < 0 \|\| total < 0 \|\| passed > total` fails; the `total == 0` free pass survives only for two present non-negative integers. Tests `verifier_pass_refuses_a_block_that_counts_nothing`, `verifier_pass_zero_total_is_one` pass. |
| CLI-143 | blocker | closed | `paths` required at load (`gates.rs:137`); `gate.rs:891-896` fails `DFA-E323` on an empty list; `min_count = 0` with `absent = false` and `min_count` with `absent = true` are both refused at load; an empty directory no longer counts (`:932-942`). Tests `file_exists_refuses_a_neutralised_min_count`, `file_exists_with_an_empty_paths_list_fails`, `file_exists_does_not_count_an_empty_directory` pass. |
| CLI-144 | blocker | closed | `gate.rs:1038-1042`: values resolve through `resolve_at` and render, so a null is a present value that fails membership; `path`, `field`, `values` are required at load. Tests `field_in_enum_null_value_fails`, `a_check_missing_a_required_key_is_e302` pass. |
| CLI-145 | blocker | closed | `gate.rs:1146-1163`: null counts as zero entries, a scalar or mapping is `DFA-E331` naming what was found, `field` is required at load (`gates.rs:140`). Test `length_between_distinguishes_null_from_a_scalar` passes. |
| CLI-146 | blocker | closed | `gate.rs:1187-1211`: an empty `docs` expansion is `DFA-E345`, `load_value` propagates, a non-sequence `open_questions` is `DFA-E324`. Tests `no_open_questions_over_an_empty_docs_list_fails`, `no_open_questions_fails_on_an_absent_document_and_a_non_list` pass. |
| CLI-147 | blocker | closed | `gate.rs:734-743` fails `DFA-E345` on an empty expansion, `:759-768` fails `DFA-E200` for a path no doc-type row covers, and `:787-796` collects every invalid document. Tests `doc_valid_over_an_empty_docs_list_fails`, `doc_valid_over_a_path_the_table_does_not_cover_fails`, `doc_valid_reports_every_invalid_document` pass. |
| CLI-148 | blocker | closed | `gate.rs:703-715`: a glob beginning with `.` enumerates from the project root and matches against `project::rel_display(&ctx.root, …)`, so the literal and glob branches share the spec-245 rule. Test `a_leading_dot_glob_resolves_against_the_project_root` (`tests/gate_check.rs:1500-1523`) passes. |
| CLI-149 | blocker | closed | `gate.rs:1517-1537`: `state_field` resolves fallibly and a miss is `DFA-E345` naming the path; `state_field` is required at load. Test `column_contains_all_separates_a_typo_from_an_empty_array` passes. |
| CLI-150 | blocker | closed | `gate.rs:1564-1604`: `substitute` is applied to all four field names, an absent or empty start is `DFA-E345`, an unparsable timestamp is `DFA-E337`. Tests `elapsed_days_fails_closed_on_both_halves`, `elapsed_days_uses_calendar_days_and_prefers_the_remedy_pair` pass. |
| CLI-151 | blocker | closed | `gate.rs:630-685`: `expect_null` and `expect_empty` take `subject_path(gate, c, id, phase)` (`:621-624`, which falls back to `gate.document` on every kind) and propagate the load error instead of returning pass. Tests `null_when_reads_the_checks_own_document_and_field`, `a_null_when_on_a_kind_with_no_field_is_e302`, `null_when_and_empty_when` pass. |
| CLI-152 | blocker | closed | `gate.rs:1332` `read_section` returns `Result<Option<Section>>` and `section_or_fail` (`:1402`) distinguishes an absent heading from an empty section. Tests `row_count_between_separates_an_absent_section_from_an_empty_one`, `row_count_between_does_not_count_a_subheading_as_a_body_line` pass. |
| CLI-153 | blocker | closed | `pattern` required at load (`gates.rs:143`) and `cli/src/pattern.rs:56-62` refuses an empty pattern with `DFA-E302`. Unit test `pattern::tests::an_empty_pattern_is_refused` and `column_matches_pattern_and_unique` pass. |
| CLI-154 | high | closed | `gate.rs:1306-1318`: the second direction is implemented — cover values naming no universe record fail `DFA-E333`, tested against every record so an excluded one is not called fabricated. Test `set_cover_reports_a_cover_value_naming_no_record` passes. |
| CLI-155 | high | closed | `gate.rs:1251-1276`: the identity is read from the leaf of `universe` and the status from a new `status_field` key defaulting to `status`. Test `set_cover_reads_the_universe_leaf` passes. |
| CLI-156 | high | closed | `gate.rs:1063-1066` `parse_date` parses against the check's `format` through `parse_strftime_borrowed` and returns a `Date`, so `after_field` orders dates and month/day ranges are validated by the parse. Test `field_is_date_parses_format_and_after_field` passes. Note: that test still never sets a non-default `format`, so the `%d/%m/%Y` case the fix spec named is unguarded. |
| CLI-157 | high | closed | `gates::parse_check` refuses `from` without `to` and either form beside `prefixes`; `gate.rs:811-813` records that the branch is reachable only with both set. Test `ids_resolve_refuses_a_half_configured_or_doubled_form` passes. |
| CLI-158 | high | closed | `gate.rs:819-829`: `from` values render (nulls included, so a null is an unresolved reference) while nulls at `to` are dropped from the target set. Test `ids_resolve_treats_a_null_at_from_as_unresolved` passes. |
| CLI-159 | high | closed | `gate.rs:962-1022`: `path` names a document on every branch and the in-document location is read from `field`. Tests `fields_present_path_names_a_document_even_with_a_gate_document`, `fields_present_with_no_field_tests_the_document_itself`, `fields_present_refuses_fields_without_a_collection` pass; `default_gates.rs::the_default_file_is_the_spec_block_byte_for_byte` still passes, so the template migration kept the fence identical. |
| CLI-200 | blocker | closed | `gate.rs:3522-3558`: `result` starts at `"FAIL"`; an absent `stories` key, a non-sequence, an empty list and an entry with no id each raise `DFA-E321`. Tests `require_release_with_an_absent_stories_key_is_e321`, `require_release_with_an_empty_stories_list_is_e321`, `require_release_with_an_entry_lacking_id_is_e321` pass. |
| CLI-201 | blocker | closed | `cli/src/cmd/phase.rs:126-140`: a predecessor named with an empty report list is refused with `DFA-E320` before any state is written. Test `phase_set_refuses_a_predecessor_that_produced_no_report` passes. |
| CLI-202 | blocker | closed | `gate.rs:3564+` `resolve_plan_subject` no longer discards the membership test; an epic no document defines is `DFA-E013`. Tests `require_plan_with_an_epic_no_document_defines_is_e013`, `require_plan_resolves_a_sprint_through_its_epic`, `require_plan_sprint_naming_an_unknown_epic_is_e013` pass. |
| CLI-203 | blocker | closed | Tests `require_plan_sprint_without_sprint_yaml_is_e013`, `require_plan_sprint_without_an_epic_key_is_e013`, `require_plan_sprint_with_an_unparsable_sprint_yaml_is_e401` pass; `require_build_with_a_sprint_lacking_an_id_is_e013` closes the sibling CLI-072. |
| CLI-204 | blocker | closed | Tests `require_discover_with_an_unparsable_decision_is_e401`, `require_discover_without_explore_exits_zero`, `require_discover_with_a_decision_for_another_idea_exits_zero` pass — the three cases are now distinguished. |
| CLI-205 | high | closed | `lib.rs:121` adds `errors: Vec<Diag>` to `Outcome`; `lib.rs:201-206` pushes them into the envelope and `:225-229` renders them to stderr. Test `a_failing_gate_carries_its_codes_in_errors` passes. |
| CLI-206 | high | closed | `cli/src/main.rs:7-36`: `wants_json()` (`:53-55`) scans the raw arguments before `try_parse`, and a non-display parse failure prints one envelope at exit 3 carrying `DFA-E010`. Test `json_parse_failure_prints_one_envelope` passes. |
| CLI-207 | high | closed | `lib.rs:158-186`: both early returns emit an envelope under `--json` (version data, and `DFA-E011` at exit 3 for a missing subcommand). Tests `json_version_prints_an_envelope`, `json_with_no_subcommand_prints_an_envelope` pass. |
| CLI-208 | high | closed | `lib.rs:190` resolves the root in `run` and `:238-243` sets it on the error envelope. Test `an_error_envelope_carries_the_project_root` passes. |
| CLI-209 | high | closed | `DFA-E420` is gone from `src/`; `aggregate.rs:149-156` emits `DFA-W420` at exit 0, naming the excluded file, and `specs/01-cli.md:777,1228` now define that row, so the code and the table agree. Tests `every_code_literal_in_src_is_a_table_row`, `aggregate_over_an_unparsable_report_warns_and_keeps_the_rest`, `aggregate_over_only_unparsable_reports_still_answers` pass. Divergence recorded: the fix spec asked for `DFA-E401` exit 1; the spec was changed to the warning band instead, with the reason stated at `:1228`. |
| CLI-210 | high | closed | `gate.rs:3348-3383` `require_id_shape` / `check_arm_id` fix one id shape per arm before the predecessor logic. Tests `require_constitute_with_a_story_id_is_e013`, `require_verify_with_an_idea_id_is_e013`, `require_plan_with_a_story_id_is_e013`, `require_release_with_an_idea_id_is_e013` pass. |

## AUDIT-2 — hooks

| ID | severity | status | evidence |
|---|---|---|---|
| HOOK-001 | high | closed | Template matcher is `"Bash\|PowerShell"` on the shell `PostToolUse` group (verified by `cat hooks/settings.hooks.json`), and `run.rs:1062` branches on `tool == "Bash" \|\| tool == "PowerShell"`. Tests `post_tool_use_powershell_matching_test_command_runs_partial`, `post_tool_use_bash_matching_test_command_runs_partial`, `hooks::settings::tests::the_shell_rule_and_the_verifier_matcher_render_from_the_project` pass. |
| HOOK-006 | high | open | The named heredoc scenario is closed: a `cat > .devforgeai/stories/…` redirection is parsed by `shell_write_targets` (`run.rs:963-986`) and denied by the shell `PreToolUse` arm. The class is not: `SHELL_WRITES` (`run.rs:578-588`) holds ten names, no `FileChanged` group exists in the template, `file-changed` is absent from `EVENTS` (`run.rs:19-27`), and no Stop-time scan exists. See Open items. |
| HOOK-010 | blocker | closed | Same evidence as CLI-003: `run.rs:1092-1118` emits `hookSpecificOutput.additionalContext` at exit 0, capped, empty-suppressed; `lib.rs:208-213` makes it the whole of stdout. |
| HOOK-011 | blocker | closed | `run.rs:1221-1244`: a blocked Stop emits `{"decision":"block","reason":…,"systemMessage":…}` where `reason` names the gate, the result and the failing check ids, and the same text is copied to `Outcome.stderr` as the degradation path. Test `stop_fail_emits_block_reason_and_system_message` passes. |
| HOOK-012 | high | closed | `run.rs:188-219`: the payload parse and every handler error route through `fail_closed` (`:221-245`), which turns a `CliError` on a blocking event into exit 2 with a deny or a block decision. Tests `pre_tool_use_unparsable_config_denies`, `hook_unparsable_payload_denies_on_a_blocking_event`, `stop_internal_error_blocks_rather_than_exiting_five` pass; `hook_stdin_not_json_gives_e021` was rewritten to assert the propagating exit 3 on the non-blocking `session-start` only (`tests/hook_run.rs:1213-1223`). |
| HOOK-020 | blocker | closed | `run.rs:67-95` defines `hook_event_name`, `deny` and `block_decision`; every handler sets `hook_json` — PreToolUse deny (`:704`), PostToolUse additionalContext (`:1112`), Stop decision/systemMessage (`:1237,1249`), SubagentStop block (`:1395`), UserPromptExpansion block, SessionStart plain stdout as the reference endorses (`:443-446`). Verified on the release binary: `hook run stop` printed exactly one JSON object. |
| HOOK-021 | blocker | closed | `run.rs:1245-1250` emits `{"systemMessage": <handoff>}` on the non-blocking Stop path, and `system_message` (`:1267-1305`) joins the cross-cutting block. Test `stop_pass_emits_handoff_as_system_message` and `stop_send_back_emits_system_message_without_block`, `stop_cross_cutting_joins_two_blocks` pass. Confirmed on the binary: the temp-repo Stop printed a `systemMessage` block and no stray stdout lines. |
| HOOK-030 | blocker | open | Same mechanism as CLI-004: `run.rs:1188-1206` blocks again while `stop_hook_active` is true up to a self-imposed budget of 3, resetting when the `(session, phase, id)` chain changes, and `specs/01-cli.md:1549` now documents that budget. Only the trust-failure branch honours the flag (`run.rs:296-302`). See Open items. |
| HOOK-031 | high | closed | `cli/src/state.rs` `[stop_hook]` carries `blocked_session`, and `run.rs:1190-1198` resets `block_count` whenever `stop_hook_active` is false or the `(session, phase, id)` chain differs, so a counter cannot survive into a later session and tolerate a FAIL for ever. Tests `stop_counter_resets_on_new_session_id`, `stop_a_fresh_turn_starts_a_new_budget`, `stop_a_different_subject_starts_a_new_budget` pass. |
| HOOK-040 | blocker | closed | Same evidence as CLI-002: `run.rs:1316-1319`; `transcript_path` no longer appears in `subagent_stop`. |
| HOOK-041 | high | closed | `run.rs:1402-1427` `last_assistant` reads the subagent's own `agent_transcript_path` and `text_of` (`:153-172`) accepts `content` as a string or as an array of typed blocks. Unit test `hooks::run::tests::text_of_reads_a_string_and_a_block_array` and `subagent_stop_reads_agent_transcript_path_when_the_message_is_absent` pass. |
| HOOK-050 | high | closed | `run.rs:59-65` `blocks()` includes `subagent-stop` (and `prompt-expansion`, `trust-check`); `trust_failure` (`:258-326`) exits 2 on those and emits `{"systemMessage": …}` with exit 4 on `session-start` and `post-tool-use`, while still writing `TRUST_FAIL` into `[last_gate]`. Tests `hook_trust_failure_blocks_on_subagent_stop`, `hook_trust_failure_warns_on_session_start`, `hook_trust_failure_exits_four_on_post_tool_use`, `trust_check_denies_every_tool_on_failure` pass. |
| HOOK-060 | high | closed | Both `PreToolUse` handlers carry `"timeout": 600` in `hooks/settings.hooks.json` and in the rendered `.claude/settings.json` of the temp-repo init (mechanical check 7 output below). |
| HOOK-070 | high | closed | `run.rs:343-365` `rel_under` canonicalises both sides (through the parent for a file that does not exist yet) and compares segments case-insensitively on Windows, so a lower-case drive letter, a short name, a junction and a UNC spelling all resolve; `:610-618` rebuilds the path from the resolved relative form before handing it to any arm, and `outside_root` (`:728-780`) refuses a write to the trust store or framework tree and refuses any unresolvable path during a Build run with a declared file set. Tests `pre_tool_use_lowercase_drive_letter_still_lints`, `pre_tool_use_denies_a_write_to_the_trust_store` pass. |
| HOOK-071 | high | closed | `run.rs:370-383`: `hook_path_key` folds case on Windows and wraps the key in slashes; `under_dot` is the single segment test and the `starts_with` spelling is gone. Tests `hooks::run::tests::hook_path_key_does_not_match_a_partial_segment`, `hooks::run::tests::the_dot_directory_test_is_case_folded_on_windows`, `pre_tool_use_mixed_case_dot_dir_hits_the_producer_check` pass. |

## Mechanical checks

**1. `cargo test --no-fail-fast` (from `cli/`)** — exit 0. Per-target results summed: **916 passed, 0 failed, 0 ignored**, across 31 test targets (30 binaries plus the doc-tests target). Full output kept at the scratchpad path `testrun.txt`. This matches the seal's claim of 916.

**1b. `cargo clippy --all-targets -- -D warnings`** — exit 0, output `Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 0.19s`. Warm cache; no diagnostic was emitted under `-D warnings`.

**5. `diff cli/templates/settings.hooks.json hooks/settings.hooks.json`** — no output, and `md5sum` gives `fa9f2a579712ca0f98d909462b16919b` for both: byte-identical. Every handler is exec form (`"command": "@@DEVFORGEAI@@"` plus an `args` array); no shell-form string survives. Shell matchers are `Bash|PowerShell` on both the `PreToolUse` and the `PostToolUse` shell groups. Events registered: `SessionStart` (matcher `startup|resume|clear|fork`), `UserPromptExpansion` (the nine skill names), `PreToolUse` (three groups: `Write|Edit|NotebookEdit`, `Bash|PowerShell`, and a `trust-check` group on `Write|Edit|NotebookEdit|Bash|PowerShell|Agent`), `PostToolUse` (`Write|Edit|NotebookEdit` with three `if` rules, and `Bash|PowerShell` with two async handlers), `Stop` (no matcher), `SubagentStop` (matcher `@@VERIFIERS@@`). No `FileChanged` group and no `PostToolUse` `Agent` group (HOOK-006, HOOK-097).

**7. `devforgeai init` in a temp git repo**, using `cli/target/release/devforgeai.exe` with `--from C:\Projects\DevForgeAI`:

- Exit 0. Output: `Copied 102 skills, 46 agents`; `Commands /build, /constitute, /design, /discover, /explore, /plan, /reflect, /release, /verify`; `Hooks .claude/settings.json merged; git hooks pre-commit, commit-msg, pre-push`; `CLAUDE.md devforgeai section written`; `Next devforgeai trust pin --framework C:/Projects/DevForgeAI, in a terminal outside Claude Code`.
- `.claude/skills/` holds exactly `build constitute design discover explore plan reflect release verify` — the nine short names.
- `CLAUDE.md` is 33 lines, opening `<!-- devforgeai:begin -->` at line 3 and closing `<!-- devforgeai:end -->` at line 33.
- `.claude/settings.json` carries `hooks` and `permissions`, with `permissions.allow = ["Bash(devforgeai *)", "PowerShell(devforgeai *)"]`. Every handler rendered as exec form with the absolute `.exe` path in forward-slash form. `@@VERIFIERS@@` rendered to the twenty registered verifier names. The two `if` rules on the shell `PostToolUse` handlers were dropped because no single `test_command` renders in a fresh project, which is the behaviour `hooks::settings::tests::the_shell_rule_is_dropped_when_no_single_test_command_renders` locks in.
- `devforgeai trust verify` → exit 4, `DFA-E501 … trust.toml not found` — fails closed.
- `printf '{"stop_hook_active":false,"session_id":"v"}' | devforgeai hook run stop` → exit 2; stdout parses as **exactly one** JSON object with keys `decision`, `reason`, `systemMessage`; `decision == "block"`; `reason` names the pin command; `systemMessage` renders `Gate TRUST FAIL DFA-E501`. Repeating with `"stop_hook_active": true` → exit 0 with `systemMessage` alone, and `state.toml [last_gate] result = "TRUST_FAIL"`.
- Probe of the shell `PreToolUse` arm in that repo returned the trust deny for every command, so the shell-write parser could not be exercised end to end there; its behaviour is recorded from the code and its unit tests instead.

**9. `cargo test --test errors_table --test default_gates`** — exit 0, both green at the time of this run.
`default_gates`: 14 passed, 0 failed — including `the_default_file_is_the_spec_block_byte_for_byte`, `the_default_file_holds_eight_gates_and_sixty_three_checks`, `a_check_missing_a_required_key_is_e302`, `a_key_outside_its_enum_is_e302`, `ids_resolve_refuses_a_half_configured_or_doubled_form`, `file_exists_refuses_a_neutralised_min_count`, `a_condition_naming_two_subjects_is_e302`, `a_null_when_on_a_kind_with_no_field_is_e302`, `a_gate_phase_outside_the_enum_is_refused`, `fields_present_refuses_fields_without_a_collection`, `every_verifier_the_default_gates_name_is_registered`.
`errors_table`: 6 passed, 0 failed — `every_spec_code_appears_once`, `exits_match_the_spec_table_row_for_row`, `the_module_carries_no_code_outside_the_spec_table`, `every_code_literal_in_src_is_a_table_row`, `warning_codes_all_exit_zero`, `the_stub_code_is_a_spec_row_in_the_exit_five_band`.
No retry was needed. Both targets read `specs/` from disk, and `specs/` is under concurrent edit, so this result is timestamped to this run.

## Open items

**CLI-001 (blocker) — the enforcing half of the shell-write guard was never built.**
Remaining: the Stop-time scan the fix spec calls "the enforcing half". `cli/src/hooks/run.rs::stop` (`:1127-1258`) runs `gate check` and `handoff` and nothing else; `[stop_hook]` has no `scanned_at` field and no code reads a modification time anywhere under `src/hooks/`. The supplementing half that did land recognises exactly two forms: a `>`/`>>` redirection target (`run.rs:963-986`) and the token after one of `tee, Out-File, Set-Content, Add-Content, New-Item, Copy-Item, Move-Item, cp, mv` (`run.rs:578-588, 989-1004`).
Reachable path: from phase `verify`, `python -c "open('.devforgeai/stories/STORY-014.md','w').write(doc)"` — or `sed -i`, `node -e`, `Rename-Item`, the `sc`/`ac` PowerShell aliases, `git checkout -- .devforgeai/…`, or any redirection built by variable expansion — produces no recognised target, so `pre_tool_use_shell` exits 0 with no action, no producer check runs, and the Stop that ends the turn never looks at the file. The written document then satisfies `doc_valid` and feeds `gate require`.

**CLI-004, CLI-061, HOOK-030 (high, high, blocker) — `stop_hook_active` is not honoured on the gate path.**
Remaining: `run.rs:1188-1206` treats the flag as a budget key rather than a stop condition. With `stop_hook_active: true` and a FAIL gate the hook blocks again (exit 2) until `STOP_BLOCK_CAP = 3` for that `(session_id, phase, id)` chain, and `:1193` resets the budget to zero whenever the chain changes. This is the shape AUDIT-1 calls a "guidance" finding: **the spec and the implementation now agree with each other and disagree with the harness reference.** `specs/01-cli.md:383` ("The triple keys the budget per continuation chain rather than per session … The budget itself is three blocks"), `:529` and `:1549` were rewritten to describe exactly this code, and the code comment at `run.rs:1135-1139` argues for it — re-running the gate on the continuation is the only way to notice that the work now passes. Against that: `specs/ANTHROPIC-GUIDANCE.md:23` is unambiguous ("A hook must read `stop_hook_active` and exit 0 when set to avoid futile loops") and, by its own header and AUDIT-1's authority note, outranks `specs/01-cli.md`; and FIX-PLAN decision 1 says "`stop_hook_active` true: `{"systemMessage": <block from the last report>}`, exit 0, no gate run". No decision record supersedes decision 1, so this cannot be closed as `superseded` — it needs either a decision that amends decision 1 or a code change. The orchestrator is the one who can settle it; the verifier can only record that the binding document and the shipped behaviour differ.
Reachable path: exactly the audit's scenario. Gate FAIL at (`explore`, `IDEA-003`) blocks. Claude continues and the continuation turn runs `phase set explore --id IDEA-004`. The next Stop arrives with `stop_hook_active: true` and a changed subject, so `same_chain` is false, the counter resets to 0, and the hook blocks again. Alternating or advancing subjects give an unbounded chain of blocks bounded only by the harness's own 8-block cap, which ends the turn with nothing shown — the failure CLI-004 and HOOK-030 describe. The same continuation also re-runs the full `gate check`, including the project's test suite, on every block (HOOK-033). Note the inconsistency: the trust-failure branch at `run.rs:296-302` *does* exit 0 when the flag is set, so the two Stop paths disagree about the same field.
CLI-061 rides on this: `tests/hook_run.rs:847-854` asserts exit 2 for `stop_hook_active: true`, which is the assertion CLI-061 asked to be deleted.

**CLI-030 (blocker) — the two descriptions of the source digest still disagree.**
Remaining: the operational half is good — I recomputed the digest independently (89 files, `sha256:2c3964003698088cd73fd75552cd83b6366ae54ccf74b4033add1ee5365a71df`) and it equals `cli/REVISION` line 2, and `cli/DIGEST` equals the sha256 of the shipped `target/release/devforgeai.exe`, so `trust verify` no longer raises `DFA-E504` on this checkout. Step one of the fix spec landed too, as the hidden `trust digest` subcommand (`cli/src/cmd/trust.rs:93-126`) rather than as `src/bin/gen-revision.rs`, so the release files are written by the same walk that reads them. But step two, convergence, is not done: `specs/01-cli.md:1580` and decision 39 at `:2913` still say "every **tracked** file under `cli/` … excluding `cli/DIGEST`", while `cli/src/trust.rs:171` excludes `cli/DIGEST` **and** `cli/REVISION` and `:194-222` walks the directory, skipping any directory named `target` or `.git` at any depth (`is_excluded_dir`, `:237-239`, was not narrowed to depth 1 as the fix spec asked). No test binds the walk to the shipped `REVISION`; the fix spec's `the_walk_digest_matches_the_shipped_revision` does not exist, and the trust tests that do exist (`source_digest_is_stable_across_runs`, `source_digest_is_order_independent_of_walk`, `source_digest_excludes_both_release_files`) all compare the algorithm with itself.
Reachable path: nothing in the spec tells a releaser to run `trust digest`, and `specs/01-cli.md:1580` still describes a different algorithm, so a release cut by following the spec — `git ls-files -- cli/` minus `cli/DIGEST` — includes `cli/REVISION` in the hash and omits nothing under a nested `target/`. The binary then recomputes a different value and every hook exits 2 or 4 with `DFA-E504` for every user, which is the finding's original failure verbatim. The missing locking test is what would catch that at build time rather than on a user's machine. `specs/` is F6's file and is under concurrent edit; the two lines above were read during this verification and may have moved since.

**CLI-060 (high) — no test observes a hook's stdout bytes.**
Remaining: `tests/hook_run.rs:140-142` reads `out.hook_json` from the in-process `Outcome`, and `grep -n "cargo_bin\|Command::new" tests/hook_run.rs` returns nothing, so the plumbing in `lib.rs:208-213` — write `hook_json` and suppress `human` and `raw` beside it — is asserted by no test at any level.
Reachable path: an edit that drops the `hook_json` arm from `lib.rs::run`, or that lets `human` print alongside it, leaves all 916 tests green while the harness rejects the hook's stdout as a schema failure ("must contain only the JSON object") and every PreToolUse deny and Stop block silently degrades to a non-blocking notice. Mitigating: I confirmed by hand that today's release binary emits exactly one object for `hook run stop` (mechanical check 7), so the product is correct at this revision; it is the guard that is missing.

**HOOK-006 (high) — the Bash-write class is narrowed, not closed.**
Remaining: the fix spec's `file-changed` event and its `FileChanged` registration were not built — `EVENTS` (`run.rs:19-27`) holds seven names and `file-changed` is not among them; `hooks/settings.hooks.json` registers no `FileChanged` group and `session_start` (`run.rs:422-451`) returns no `watchPaths`. The finding's own scenario (`cat > .devforgeai/stories/STORY-007.md <<'EOF'`) is now denied, so the hole is much smaller than it was.
Reachable path: the same residue as CLI-001 — any write through a program the ten-name list and the redirection scan do not recognise reaches `.devforgeai/` with no producer check, and nothing after the fact observes it.

## Regressions

**1. The shell `PostToolUse` group spawns twice per shell call in a fresh project.** In the rendered `.claude/settings.json` from mechanical check 7, the `Bash|PowerShell` group holds two handlers that are now byte-identical (`hook run post-tool-use`, `async: true`, `timeout: 900`) because `hook install` dropped the `if` rule from both when no single `test_command` rendered. Every `ls`, `git status` and `cargo build` therefore starts two dispatcher processes, each of which hashes the binary for `trust verify` and loads `config.toml`. Before the fix wave there was one handler with a `Bash` matcher. This is the cost half of HOOK-002 doubled; it is not a correctness defect and it disappears once a project's `test_command` renders, but the second handler is pure waste in either state and one of the two should be dropped by `hook install`.

**2. A subcommand exists that no spec defines.** `cli/src/cli.rs:386-396` adds a hidden `trust digest`, and `grep -n "trust digest" specs/01-cli.md` returns nothing: the `## CLI calls` sections, the release paragraph at `:1580` and the subcommand list all predate it. AUDIT-1's "Subcommand conformance" preamble recorded "no subcommand exists that the spec does not define" as a property that held before the fix wave; it no longer does. The command itself is right — it is exactly the CLI-030 remedy, writing the release digests from the same code path that reads them — so the fix is to document it in `specs/01-cli.md:1580` as the release step, not to remove it. Two smaller defects in it while that is done: `cli/src/cmd/trust.rs:113` hardcodes line 1 to `REVISION_UNVERSIONED`, so the `git rev-parse HEAD` value spec 1580 requires is never written and `--version` can never distinguish two builds (the source of CLI-070, out of scope here); and `:122-123` labels both printed lines `REVISION`, so a reader cannot tell line 1 from line 2.

Nothing else broke: `default_gates::the_default_file_is_the_spec_block_byte_for_byte` still passes, so the `gates.toml` template and the spec fence are still byte-identical after the `fields_present` migration of CLI-159; `errors_table` still asserts a two-way bijection with the spec table and `every_code_literal_in_src_is_a_table_row` now closes the reverse direction that let `DFA-E420` ship; no test is `#[ignore]`d and the suite is 916 green.
