apiVersion: v1
kind: Service
metadata:
  name: "@@APP@@"
  labels:
    app: "@@APP@@"
spec:
  type: ClusterIP
  selector:
    app: "@@APP@@"
  ports:
    - name: http
      port: 80
      targetPort: @@PORT@@
